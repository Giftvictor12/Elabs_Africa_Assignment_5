// replace in javascript 

// the replace() method is use to replace a string or substring in a string. 
// The replace() method returns a new string with the replacement


let originalString = "The white shirt looks good on me.";

let newString = originalString.replace("white", "red");

console.log(newString);

